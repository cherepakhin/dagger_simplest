package ru.perm.v.dagger_rest.modules;

import dagger.Module;
import dagger.Provides;
//import ru.perm.v.dagger_rest.dto.VacancyDTO;

@Module
public class VacancyModule {
    /*
    @Provides
    VacancyDTO provideEmptyVacany() {
        return new VacancyDTO();
    }

    @Provides
    public VacancyDTO getById(Long id) {
        return new VacancyDTO(id, "Vacancy" + id);
    }
     */
}
